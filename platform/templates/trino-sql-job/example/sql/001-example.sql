-- Replace this file with your SQL statements.
-- Add more files numbered sequentially (002-foo.sql, 003-bar.sql, etc.)
-- and list them in kustomization.yaml under configMapGenerator.files.
SELECT 1;
