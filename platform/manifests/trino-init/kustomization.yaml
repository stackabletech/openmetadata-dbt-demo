---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../templates/trino-sql-job

namePrefix: trino-init-

configMapGenerator:
  - name: trino-sql-files
    files:
      - sql/001-create-hive-schema.sql
      - sql/002-create-tables.sql
      - sql/003-create-iceberg-schema.sql
      - sql/004-create-iceberg-lakekeeper-schema.sql

generatorOptions:
  disableNameSuffixHash: true

# Configure Trino endpoint and/or authentication by uncommenting below.
# patches:
#   - path: trino-config.yaml