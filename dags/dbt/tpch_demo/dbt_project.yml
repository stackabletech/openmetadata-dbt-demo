name: tpch_demo
version: "1.0.0"
config-version: 2

profile: tpch_demo

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  tpch_demo:
    staging:
      +materialized: view
    marts:
      +materialized: table
